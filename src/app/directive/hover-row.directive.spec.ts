import { HoverRowDirective } from './hover-row.directive';

describe('HoverRowDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverRowDirective();
    expect(directive).toBeTruthy();
  });
});
